{
  "name": "mcp-gateway",
  "version": "0.3.0",
  "description": "MCP API Gateway - intelligent multi-server proxy with caching, resilience, and usage monitoring. Supports Streamable HTTP, SSE, and stdio transports for universal agent compatibility.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-gateway": "dist/stdio.js"
  },
  "scripts": {
    "build": "tsc",
    "build:all": "tsc && cd dashboard && npm run build",
    "start": "node dist/index.js",
    "start:stdio": "node dist/stdio.js",
    "dev": "tsx src/index.ts",
    "dev:stdio": "tsx src/stdio.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "express": "^5.2.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
